name: palettes
description: A new Flutter package project.
version: 1.0.0
repository: https://github.com/albemala/flutter_palettes

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  buffer: ^1.2.2
  archive: ^3.4.10

  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1

  very_good_analysis: ^5.1.0

flutter:
